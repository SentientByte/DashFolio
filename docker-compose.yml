version: "3.9"
services:
  dashfolio:
    build: .
    ports:
      - "5000:5000"
    environment:
      FLASK_ENV: production
      DASHFOLIO_DATA_DIR: /config
    volumes:
      - /mnt/config/dashfolio:/config
    restart: unless-stopped
